import { InvalidationInterceptor } from './invalidation.interceptor';

describe('InvalidationInterceptor', () => {
  it('should be defined', () => {
    expect(new InvalidationInterceptor()).toBeDefined();
  });
});
